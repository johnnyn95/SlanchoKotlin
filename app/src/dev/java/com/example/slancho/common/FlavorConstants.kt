package com.example.slancho.common

import com.example.slancho.api.Environment

/**
 * Flavor-specific constants for the different types of builds.
 */
class FlavorConstants {
    companion object {
        val ENV: Environment = Environment.Environments.DEV
    }
}